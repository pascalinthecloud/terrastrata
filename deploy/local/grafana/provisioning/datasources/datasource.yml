apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus # referenced by the provisioned dashboard
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 5s # match the Prometheus scrape interval
