apiVersion: v1
kind: Service
metadata:
  name: {{ include "terrastrata.fullname" . }}
  labels:
    {{- include "terrastrata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "terrastrata.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
